# Port targets — `rama-tcp-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `rama-tcp` that codex-rs uses, and therefore the minimum subset that `rama-tcp-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-111] (rank `59`, tier `direct`, status `To Do`)

**Cargo package name**: `rama-tcp`
**Rust module name (in `use` statements)**: `rama_tcp`

## Summary

- **5** distinct symbols imported by codex-rs
- **8** total `use` statements
- **3** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `rama_tcp::client::Request`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`
- Aliases used: `as TcpRequest`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/http_proxy.rs:69` (`codex-network-proxy`) — `use rama_tcp::client::Request as TcpRequest`
- `network-proxy/src/socks5.rs:37` (`codex-network-proxy`) — `use rama_tcp::client::Request as TcpRequest`

### `rama_tcp::server::TcpListener`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/http_proxy.rs:70` (`codex-network-proxy`) — `use rama_tcp::server::TcpListener`
- `network-proxy/src/socks5.rs:38` (`codex-network-proxy`) — `use rama_tcp::server::TcpListener`

### `rama_tcp::TcpStream`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/connect_policy.rs:11` (`codex-network-proxy`) — `use rama_tcp::TcpStream`
- `network-proxy/src/socks5.rs:36` (`codex-network-proxy`) — `use rama_tcp::TcpStream`

### `rama_tcp::client::TcpStreamConnector`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/connect_policy.rs:12` (`codex-network-proxy`) — `use rama_tcp::client::TcpStreamConnector`

### `rama_tcp::client::service::TcpConnector`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/connect_policy.rs:13` (`codex-network-proxy`) — `use rama_tcp::client::service::TcpConnector`
